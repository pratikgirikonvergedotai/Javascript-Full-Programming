let person = {
    name : "Pratik",
    age : 20,
    country : "Japan"
}

function logData(){
    return `${person.name} is ${person.age} years old and lives in ${person.country}`
}

console.log(logData())